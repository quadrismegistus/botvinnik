node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Raw third-party datasets stay local (CC BY-NC). The derived
# static/commentary.json is committed; regenerate with scripts/build-commentary.py
/data


# Tauri build artifacts and the 109MB engine binary — never commit these
# (tracked config lives under src-tauri/ on the tauri branch only)
src-tauri/target/
src-tauri/binaries/
src-tauri/gen/

# Playwright
/test-results
/playwright-report
# the flutter e2e config lives beside its tests, so its output does too
flutter/test-results/
flutter/playwright-report/
scripts/wasm-engine/
scripts/maia-spike/
scripts/maia3-spike/
scripts/engines/
data/puzzles/*.zst

# Flutter spike (flutter/) — generated/build noise; Dart tooling
flutter/build/
flutter/.dart_tool/
flutter/.flutter-plugins*
flutter/ios/Pods/
flutter/ios/.symlinks/
flutter/macos/Pods/

# bundled engine binary (113MB, GPL — staged locally, never committed)
flutter/macos/Runner/Resources/stockfish
# retro engine binaries (MIT, built by stage-macos-engines.sh, never committed)
flutter/macos/Runner/Resources/retro/

# flutter web spike: engine + brain copies, never committed
flutter/web/wasm/
flutter/web/retro/
flutter/web/garbo/
flutter/web/maia/
flutter/web/brain.js

# sqflite's shared-worker build: unused (db_init_web uses the no-worker
# factory) and regenerable with `dart run sqflite_common_ffi_web:setup`
flutter/web/sqflite_sw.js
